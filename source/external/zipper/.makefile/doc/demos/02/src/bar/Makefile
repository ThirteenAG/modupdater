PROJECT_NAME := TutorialMyMakefile
PROJECT_VERSION := 0.1.0
TARGET_NAME := bar
TARGET_DESCRIPTION := library bar
COMPILATION_MODE := release

P := ../..
M := ../../../../..
BUILD := ../../build
include $(M)/project/Makefile

INCLUDES := $(P)/include
LIB_FILES := bar.cpp

include $(M)/rules/Makefile
